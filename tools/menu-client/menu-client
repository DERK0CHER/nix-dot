#!/usr/bin/env python3
"""Entry point. Runs straight from the checkout, no install step."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from menu_client.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
