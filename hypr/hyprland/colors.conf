# Adwaita-ish colours (borders are 0px wide, kept for groups/pinned)
general {
    col.active_border = rgba(3584e4aa)
    col.inactive_border = rgba(ffffff1a)
}

misc {
    background_color = rgba(1e1e1eff)
}

windowrule = border_color rgba(FFFFFFAA) rgba(FFFFFF77), match:pin on
