# ######## Window rules — Hyprland 0.54 syntax ########
# Format: windowrule = RULE [VALUE] filtertype:filtervalue
# Format: layerrule  = RULE [VALUE] namespace_glob
# No commas. Space-separated. Colon for window filters.


# Tiling
windowrule = tile class:^dev\.warp\.Warp$

# Picture-in-Picture
windowrule = float           title:^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$
windowrule = keep_aspect_ratio title:^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$
windowrule = move 73% 72%   title:^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$
windowrule = size 25%       title:^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$
windowrule = pin             title:^([Pp]icture[-\s]?[Ii]n[-\s]?[Pp]icture)(.*)$

# Dialog windows
windowrule = float  title:^(Open File)(.*)$
windowrule = float  title:^(Select a File)(.*)$
windowrule = float  title:^(Choose wallpaper)(.*)$
windowrule = float  title:^(Open Folder)(.*)$
windowrule = float  title:^(Save As)(.*)$
windowrule = float  title:^(Library)(.*)$
windowrule = float  title:^(File Upload)(.*)$
windowrule = center title:^(Open File)(.*)$
windowrule = center title:^(Select a File)(.*)$
windowrule = center title:^(Choose wallpaper)(.*)$
windowrule = center title:^(Open Folder)(.*)$
windowrule = center title:^(Save As)(.*)$
windowrule = center title:^(Library)(.*)$
windowrule = center title:^(File Upload)(.*)$

# Tearing (games / wine)
windowrule = immediate title:.*\.exe
windowrule = immediate class:^(steam_app_)

# No shadow on tiled windows
windowrule = no_shadow floating:0

# Kitty: clear (non-frosted) transparency — disable window blur
windowrule = no_blur 1, match:class ^(kitty)$

# Solo tiled window on a workspace: drop rounding so it feels fullscreen

# ######## Workspace rules ########
workspace = special:special, gapsout:30

# ######## Layer rules — Hyprland 0.55 syntax ########
# Format: layerrule = EFFECT VALUE, match:namespace REGEX
# (comma-separated fields; every effect needs a value; namespace is a regex)

layerrule = xray 1, match:namespace .*
layerrule = animation off, match:namespace selection
layerrule = animation off, match:namespace hyprpicker
layerrule = animation off, match:namespace anyrun

# Waybar
layerrule = blur 1, ignore_alpha 0.3, match:namespace waybar

# Notifications (dunst)
layerrule = blur 1, ignore_alpha 0.5, match:namespace notifications

# Wofi / launcher
layerrule = blur 1, ignore_alpha 0.3, match:namespace launcher

# Lock screen
layerrule = blur 1, match:namespace logout_dialog

# GTK layer shell popovers
layerrule = blur 1, ignore_alpha 0.0, match:namespace gtk-layer-shell
