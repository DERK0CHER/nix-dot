# ######## Wallpaper ########
# hyprpaper 0.8+ is IPC-only (no config file)
exec-once = hyprpaper
# Apply the wallpaper to every connected output, whatever they are called.
# Hardcoding names broke the moment the second machine had DP-2 instead of HDMI-A-1.
exec-once = sleep 1 && hyprctl monitors -j | jq -r '.[].name' | while read -r m; do hyprctl hyprpaper wallpaper "$m,~/.config/hypr/wallpaper.jpeg"; done

# ######## Shell (bar, quick settings, notifications, OSD) ########
exec-once = qs -c hyprshell

# ######## Session ########
exec-once = dbus-update-activation-environment --systemd --all
exec-once = sleep 1 && dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP HYPRLAND_INSTANCE_SIGNATURE
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 || /usr/libexec/polkit-gnome-authentication-agent-1 || /run/current-system/sw/libexec/polkit-gnome-authentication-agent-1 || systemctl --user start hyprpolkitagent
exec-once = hypridle

# ######## Input method ########
# Input method, only if present: the cachy host has no fcitx5 installed and a
# failing exec-once would just log noise every login.
exec-once = command -v fcitx5 >/dev/null && fcitx5

# ######## Audio ########
exec-once = easyeffects --gapplication-service

# ######## Clipboard history ########
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# ######## Night light ########
# location: ~/.config/gammastep/config.ini (or geoclue)
exec-once = gammastep

# ######## GNOME / libadwaita look (adw-gtk3 + Adwaita) ########
exec-once = gsettings set org.gnome.desktop.interface gtk-theme 'adw-gtk3-dark'
exec-once = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
exec-once = gsettings set org.gnome.desktop.interface icon-theme 'Adwaita'
exec-once = gsettings set org.gnome.desktop.interface cursor-theme 'Adwaita'
exec-once = gsettings set org.gnome.desktop.interface cursor-size 24
exec-once = if fc-list | grep -qi 'Adwaita Sans'; then f='Adwaita Sans'; else f='Cantarell'; fi; gsettings set org.gnome.desktop.interface font-name "$f 11"; gsettings set org.gnome.desktop.interface document-font-name "$f 11"
exec-once = if fc-list | grep -qi 'Adwaita Mono'; then f='Adwaita Mono'; else f='Fira Mono'; fi; gsettings set org.gnome.desktop.interface monospace-font-name "$f 11"
exec-once = gsettings set org.gnome.desktop.wm.preferences button-layout ':'

# ######## Directories ########
exec-once = mkdir -p ~/Videos/Recordings ~/Pictures/Screenshots

# Global menu: load the appmenu plugin so Qt apps can announce their menu bar.
# Not persistent otherwise - a plugin lives only as long as the compositor
# process, so without this line the menu bar is empty after every login.
# The path is absolute because exec-once does not expand ~ reliably here.
exec-once = hyprctl plugin load $HOME/Dokumente/hyprland/nix-dot/plugins/hypr-appmenu/hypr-appmenu.so

# The menu registrar must own com.canonical.AppMenu.Registrar BEFORE any Qt app
# starts: plasma-integration checks for that name and exports nothing at all
# without it - not even over the Wayland protocol. Measured, not assumed.
exec-once = python3 $HOME/Dokumente/hyprland/nix-dot/tools/appmenu-probe/registrar-stub.py
