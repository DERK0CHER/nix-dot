# Wallpaper (swww) + wallust theming. Restores the last picked wallpaper, or
# falls back to the bundled one and themes from it on first run.
exec-once = swww-daemon
exec-once = sleep 1 && ~/.config/scripts/wallpaper-picker.sh "$(cat ~/.cache/wallust/last-wallpaper 2>/dev/null || echo ~/.config/hypr/wallpaper.jpeg)"

# Bar & notifications
exec-once = waybar
exec-once = dunst -config ~/.cache/wallust/dunstrc
exec-once = blueman-applet

# waylandar calendar widget — enable after setup (see THEMING.md):
# run `waylandar-init-theme`, configure a calendar source, then uncomment:
# exec-once = waylandar-widget

# Gamma / night light
exec-once = /usr/lib/geoclue-2.0/demos/agent & gammastep

# Input method
exec-once = fcitx5

# Core components (authentication, lock screen)
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 || /usr/libexec/polkit-gnome-authentication-agent-1
exec-once = hypridle
exec-once = dbus-update-activation-environment --all
exec-once = sleep 1 && dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Audio
exec-once = easyeffects --gapplication-service

# Clipboard history
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Cursor
exec-once = hyprctl setcursor Adwaita 24

# GTK theming
exec-once = gsettings set org.gnome.desktop.interface gtk-theme 'Adwaita'
exec-once = gsettings set org.gnome.desktop.interface icon-theme 'Adwaita'
exec-once = gsettings set org.gnome.desktop.interface cursor-theme 'Adwaita'
exec-once = gsettings set org.gnome.desktop.interface cursor-size 24
exec-once = gsettings set org.gnome.desktop.interface monospace-font-name 'Fira Mono 13'

# Ensure recordings directory exists
exec-once = mkdir -p ~/Videos/Recordings ~/Pictures/Screenshots
